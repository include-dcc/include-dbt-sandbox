# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: "include_dbt_sandbox"
config-version: 2
version: "1.0.0"

# This setting configures which "profile" dbt uses for this project.
profile: "include_dbt_sandbox"

# These configurations specify where dbt should look for different types of
# files. The `model-paths` config, for example, states that models in this
# project can be found in the "models/" directory. You probably won't need to
# change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target" # directory which will store compiled SQL files
clean-targets: # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# In this example config, we tell dbt to build all models in the example/
# directory as views. These settings can be overridden in the individual model
# files using the `{{ config(...) }}` macro.
models:
  # TODO: update the model name below to match name above
  include_dbt_sandbox:
      +materialized: table

# Grant access to schemas for analyst groups and service accounts.
# TODO: add new schemas and roles as needed.
# on-run-end:
  # Grant on base schema
 # - 'grant usage on schema "{{ target.schema }}" to role_1, role_2, role_3, role_4, role_5'
 # - 'grant select on all tables in schema "{{ target.schema }}" to role_1, role_2, role_3, role_4, role_5'

seeds:
  quote_columns: true
  schema: import
