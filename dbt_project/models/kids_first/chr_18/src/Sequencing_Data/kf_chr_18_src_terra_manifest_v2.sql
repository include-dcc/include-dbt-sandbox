{{ config(materialized='table') }}

    select
    ROW_NUMBER() OVER () AS kf_index,
    "entity:sample_id"::text as sample_id,
    "project"::text as project,
    "collaborator_sample_id"::text as collaborator_sample_id,
    "version"::text as version,
    "data_type"::text as data_type,
    "pdo"::text as pdo,
    "release_date"::date as release_date,
    "reference_sequence_name"::text as reference_sequence_name,
    "collaborator_participant_id"::text as collaborator_participant_id,
    "cram_path"::text as cram_path,
    "crai_path"::text as crai_path,
    "md5_path"::text as md5_path,
    "root_sample_id"::text as root_sample_id,
    "mean_coverage"::float as mean_coverage,
    "adapter_rate"::float as adapter_rate,
    "pf_hq_aligned_q20_bases"::integer as pf_hq_aligned_q20_bases,
    "pf_hq_aligned_reads"::integer as pf_hq_aligned_reads,
    "pf_mismatch_rate"::float as pf_mismatch_rate,
    "pf_noise_reads"::integer as pf_noise_reads,
    "pf_reads"::integer as pf_reads,
    "pf_reads_aligned"::integer as pf_reads_aligned,
    "total_reads"::integer as total_reads,
    "mean_read_length"::integer as mean_read_length,
    "pf_reads_rate"::integer as pf_reads_rate,
    "pf_reads_aligned_rate"::float as pf_reads_aligned_rate,
    "pf_aligned_bases"::integer as pf_aligned_bases,
    "reads_aligned_in_pairs"::integer as reads_aligned_in_pairs,
    "contamination_rate"::integer as contamination_rate,
    "genome_territory"::integer as genome_territory,
    "exc_baseq_rate"::float as exc_baseq_rate,
    "exc_dupe_rate"::float as exc_dupe_rate,
    "exc_mapq_rate"::integer as exc_mapq_rate,
    "exc_overlap_rate"::float as exc_overlap_rate,
    "10x_rate"::float as 10x_rate,
    "20x_rate"::float as 20x_rate,
    "exc_total_rate"::float as exc_total_rate,
    "exc_unpaired_rate"::float as exc_unpaired_rate,
    "chimera_rate"::float as chimera_rate,
    "het_snp_q"::integer as het_snp_q,
    "het_snp_sensitivity"::float as het_snp_sensitivity,
    "library-1_estimated_library_size"::integer as library-1_estimated_library_size,
    "library-1_mean_insert_size"::float as library-1_mean_insert_size,
    "library-1_name"::text as library-1_name,
    "library-1_pair_orientation"::text as library-1_pair_orientation,
    "library-1_pct_exc_dupe"::integer as library-1_pct_exc_dupe,
    "library-1_percent_duplication"::float as library-1_percent_duplication,
    "library-1_read_pairs"::integer as library-1_read_pairs,
    "mean_insert_size"::float as mean_insert_size,
    "median_coverage"::integer as median_coverage,
    "median_insert_size"::integer as median_insert_size,
    "pf_hq_aligned_bases"::integer as pf_hq_aligned_bases,
    "pf_hq_error_rate"::float as pf_hq_error_rate,
    "pf_reads_improper_pairs"::integer as pf_reads_improper_pairs,
    "read_pair_duplicates"::integer as read_pair_duplicates,
    "read_pair_optical_duplicates"::integer as read_pair_optical_duplicates,
    "reads_aligned_in_pairs_rate"::float as reads_aligned_in_pairs_rate,
    "sample"::text as sample,
    "strand_balance"::float as strand_balance,
    "width_of_10_percent"::integer as width_of_10_percent,
    "width_of_50_percent"::integer as width_of_50_percent,
    "width_of_99_percent"::integer as width_of_99_percent
from {{ source('chr_18', 'Terra_Manifest_v2') }}
    